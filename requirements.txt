# Core Dependencies
anthropic>=0.39.0
python-dotenv>=1.0.0

# TTS Engines
pyttsx3>=2.90  # Windows built-in TTS (works offline, no setup needed)
piper-tts>=1.2.0  # Optional: Better quality but harder to set up

# Optional: For advanced TTS alternatives
# TTS>=0.22.0  # Coqui TTS (more voices, but slower)

# Optional: Translation support
# argostranslate>=1.9.0
# translators>=5.9.1

# Optional: For subtitle generation
# pysrt>=1.1.2
# stable-ts>=2.0.0

# Optional: For batch processing optimization
# tqdm>=4.66.0  # Progress bars
# joblib>=1.3.0  # Parallel processing

# Optional: For advanced video analysis
# opencv-python>=4.8.0
# Pillow>=10.0.0

# Note: FFmpeg and FFprobe must be installed separately
# Install via system package manager:
# - Ubuntu/Debian: sudo apt install ffmpeg
# - macOS: brew install ffmpeg
# - Windows: Download from https://ffmpeg.org/download.html